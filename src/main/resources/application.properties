spring.application.name=loginyregister

#Configuracion del security

spring.security.user.name=admin
spring.security.user.password=Bigrado123

# Database Configuration

spring.datasource.url=jdbc:postgresql://localhost:5432/loginyregister
spring.datasource.username=postgres
spring.datasource.password=123456789
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true


# Logging
logging.level.com.exe.loginyregister=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Desactiva cach? para ver cambios sin reiniciar
spring.thymeleaf.cache=false

# Ubicaci?n y extensi?n por defecto
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# (Opcional) Modo de plantillas
spring.thymeleaf.mode=HTML

logging.level.org.thymeleaf=TRACE

# Puerto salida
#server.port=8085

# ------------------------------
# Configuración de Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=edusyncdt@gmail.com
spring.mail.password=sqwfhnjjnhhowuiv
spring.mail.protocol=smtp
# Propiedades SMTP esenciales
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Configuración adicional de seguridad
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
# Configuración adicional recomendada
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.smtp.from=edusyncdt@gmail.com
spring.mail.default-encoding=UTF-8
# URL base de la aplicación (para los enlaces en el email)
app.base-url=http://localhost:8080
# Para desarrollo - mostrar emails en consola en lugar de enviarlos
#spring.mail.host=localhost
#spring.mail.port=1025
# Configuración adicional de seguridad
